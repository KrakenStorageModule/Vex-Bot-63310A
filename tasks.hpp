/*
Contains all the task function declarations, such as the drivetrain and subsystems handler tasks
*/
#pragma once
#include "main.h"

extern int DrivetrainTask(void*); // The drivetrain task
extern int SubsystemsTask(void*); // The subsystems task